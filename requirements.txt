# Optional model clients: install only the one matching LLM_PROVIDER.
# Offline mode and the native Ollama backend need neither (standard library only).
google-generativeai>=0.7.0    # LLM_PROVIDER=gemini  (GEMINI_API_KEY)
openai>=1.30.0                # LLM_PROVIDER=openai  (OPENAI_API_KEY [+ OPENAI_BASE_URL])
