# Core — one of these LLM clients is needed; both are optional if LLM_OFFLINE=1
google-generativeai>=0.7.0    # Gemini API  (set GEMINI_API_KEY)
openai>=1.30.0                # OpenAI-compatible (set OPENAI_API_KEY; works with Ollama too)

# Standard library only — no other runtime dependencies
